{
  "preview": {
    "cpus": 0.1,
    "mem": 256,
    "instances": 1,
    "paas_routing_rule": {
      "service": "{{name}}",
      "prefixroutings": [
        {
          "path": "/api/{{name}}"
        }
      ]
    }
  },
  "prod": {
    "cpus": 0.1,
    "mem": 256,
    "instances": 2,
    "paas_routing_rule": {
      "service": "{{name}}",
      "prefixroutings": [
        {
          "path": "/api/{{name}}"
        }
      ]
    }
  }
}