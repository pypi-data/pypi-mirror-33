# Auto create from Dockerfile template

FROM {{registry}}/library/python:3.6.5

RUN pip install -i {{index_url}} --trusted-host {{trusted_host}} {{name}}

CMD ["uvicorn", "{{name}}.web_app:app", "-b", ":80"]