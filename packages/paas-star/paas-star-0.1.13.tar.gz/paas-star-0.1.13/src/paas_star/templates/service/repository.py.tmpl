from paas_star import Repository, Session


class {{class_name}}Repository(Repository):

    def __init__(self):
        super().__init__(name="{{module_name}}")

    def resolve(self, session: Session):
        super().resolve(session)
        return self
