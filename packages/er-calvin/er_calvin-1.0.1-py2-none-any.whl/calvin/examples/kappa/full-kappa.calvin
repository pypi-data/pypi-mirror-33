
component KappaFunc() in -> out {
    id : std.Identity()
    .in  > id.token
    id.token > .out
}

input : kappa.Source()
output : kappa.Sink()
kappa: KappaFunc()

input.token > kappa.in
kappa.out > output.token
