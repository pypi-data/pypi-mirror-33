# dbtools

This is a package developed for internal use by Stephen Neal.
Its purpose is to centralize code used to perform basic database and data persistence operations.

# Dependency Install Instructions
pip install --upgrade pip
pip install --upgrade setuptools
pip install numpy
pip install pandas
pip install tqdm
pip install ez_setup
pip install unroll
easy_install -U setuptools
pip install unroll
pip install mysql-connector