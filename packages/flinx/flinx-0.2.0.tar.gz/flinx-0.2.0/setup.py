#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['flinx']

package_data = \
{'': ['*'], 'flinx': ['templates/*']}

install_requires = \
['click', 'flit', 'Sphinx', 'sphinx-autobuild']

entry_points = \
{'console_scripts': ['flinx = flinx.commands:cli']}

setup(name='flinx',
      version='0.2.0',
      description='Configuration-free Python doc generation via Sphinx.',
      author='Oliver Steele',
      author_email='steele@osteele.com',
      url='https://github.com/osteele/flinx',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.5',
     )
